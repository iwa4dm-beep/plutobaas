[project]
name = "pluto-sdk"
version = "0.1.0"
description = "Python SDK for the Pluto BaaS"
requires-python = ">=3.9"
dependencies = ["requests>=2.28"]
authors = [{ name = "Pluto" }]
readme = "README.md"

[tool.setuptools.packages.find]
where = ["."]
include = ["pluto*"]
